





<div class="p-4 sm:w-1/2 lg:w-1/3 w-full " style="width:500px">
<div class=" flex items-center  justify-between p-4  rounded-lg bg-white shadow-indigo-50 shadow-md" style="height:200px">
  <div style="text-align:left">
    <h2 class="text-gray-900 text-lg font-bold"><%= buy.product.name %></h2>
    <h3 class="mt-2 mb-2 text-xl font-bold text-green-500 text-left"><%= buy.product.price %>$</h3>
    <% if buy.product.status != "delivered" %>
    <p class="text-sm font-semibold text-gray-400">Ordered by: <%= buy.customer.name %></p>
    <%else %>
    <p class="text-sm font-semibold text-gray-400">Bought by: <%= buy.customer.name %></p>
   <%end%>
    <% if Product.find_by(id: buy.product.id,status: "ordered") %>
    <%= button_to "Send" , send_order_path(id: buy.product.id ), class:"text-sm mt-6 px-4 py-2 bg-orage-300  text-white rounded-lg font-laonoto tracking-wider hover:bg-indigo-500 outline-none" %>
  <% end %>
  <% if Product.find_by(id: buy.product.id,status: "sent")  %>
     <p style="color:green;margin-top:30px"> Sent</p>
  <% end %>
   <% if Product.find_by(id: buy.product.id,status: "delivered")  %>
     <p style="color:green;margin-top:30px"> Its delivered.</p>
  <% end %> 
  </div>
  <div
    class="bg-gradient-to-tr  to-indigo-600 w-32 h-32  rounded-full shadow-2xl shadow-[#304FFE] border-white  border-dashed border-2  flex justify-center items-center ">
    <div>
    <% if buy.product.image.attached? %>
    <%= image_tag buy.product.image, class:"w-32 h-32 rounded-full object-cover object-center" %>
  <% else %>
    <%= image_tag "default_product", class:"w-32 h-32 rounded-full object-cover object-center" %>
  <% end %>
  
    </div>
    
  </div>
  <% if buy.product.review != nil %>
    <div class="my-auto mx-5 p-2 border rounded">
      Review: <br>
      <%= buy.product.review.text %>
      <%= button_to "delete", review_path(buy.product.review), method: :delete, class: "mt-2 rounded-lg py-3 px-5 bg-gray-100 font-medium" %>
    </div>
  
  <% end %>
</div>

</div>




